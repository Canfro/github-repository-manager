use std::path::PathBuf;

enum SourceType {
    GITHUB,
}

fn pull_source(source: &str, source_type: SourceType) {
    todo!()
}

fn build_package(package_id: &str, source_code: PathBuf) {
    todo!()
}

fn main() {
    todo!()
}
